@model IEnumerable<Coursework.Models.AlbumType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AlbumTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AlbumTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlbumTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlbumTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlbumTypeId })
        </td>
    </tr>
}

</table>
