@model IEnumerable<Coursework.Models.AlbumArtist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Albums.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Artists.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Albums.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Artists.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlbumArtistId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlbumArtistId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlbumArtistId })
        </td>
    </tr>
}

</table>
